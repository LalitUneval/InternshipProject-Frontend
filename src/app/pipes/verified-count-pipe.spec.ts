import { VerifiedCountPipe } from './verified-count-pipe';

describe('VerifiedCountPipe', () => {
  it('create an instance', () => {
    const pipe = new VerifiedCountPipe();
    expect(pipe).toBeTruthy();
  });
});
